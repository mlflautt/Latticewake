# Native app boundary

`swift build` builds a macOS SwiftUI shell and a narrow C++ bridge. It is not
yet an audible app: Cycle 010 must admit a fixed-capacity render kernel before
the Start control is enabled.
