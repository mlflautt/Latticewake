# Cycle 028 handoff

Base: f9d65da. The old starter is a constant -1 table before DC filtering;
its sustained filter output decays below 1e-20 after one second. That explains
the reported same-sounding thumps. A release envelope alone cannot correct it.

The explicit playable starter changes terrain zoom and path geometry; saved
scenes are untouched. Flat prepared tables become silence. The UI warns about
flat scene snapshots and offers the unsaved starter. Applying edits to that
starter does not overwrite the previously loaded file.

Measured at 48 kHz: C4 262.295 Hz, E4 328.767 Hz, G4 393.443 Hz;
sustained RMS approximately .022, within the 1% pitch threshold. The WAV demo
is generated by tests/playable_tests.cpp from the portable renderer.

Checks: make test; make test BUILD_DIR=build/sanitized with ASan/UBSan;
swift test; bundle rebuild/signature check. New Swift fixtures exercise keyboard
repeat/focus release and the same render-block factory used by the source node
from a background thread, producing matching nonzero stereo samples.

Actual device smoke through LatticewakeAudio.start/stop:
sample_rate=48000 callbacks=120 frames=56448 max_render_ns=193334
deadline_misses=0 rejected_blocks=0. This is a brief device run, not full
callback admission or a human listening verdict. Opt in with LW_DEVICE_SMOKE=1
swift test --filter optInDeviceSmoke. Full native keyboard/gesture UI review is
still pending; automated input-state checks are not claimed as that review.

Rollback: revert this cycle commit and rebuild. No source scene migration.
Remaining implementation: accepted cycles 029, 030, 031.
