# Cycle 010 — real-time kernel candidate

Precomputes a terrain table outside rendering and renders fixed-capacity voices
from bounded events. This is an admission candidate only; allocation/lock
instrumentation and device stress evidence remain required before callback use.
