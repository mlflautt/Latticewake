# Cycle 011 — audible standalone shell

Adds a buildable AVAudioEngine audition path, start/stop, pitch buttons, and
clear status. It is a host-level prototype, not evidence that the C++ terrain
kernel has passed callback admission or that its sound has been heard.
