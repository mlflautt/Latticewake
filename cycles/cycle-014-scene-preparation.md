# Cycle 014 — canonical Scene preparation

Routes canonical Scene v0 JSON through the C ABI parser and terrain-table
preparation path. The app uses a bundled valid demo scene; user scene selection
and terrain-frame UI remain future work.
