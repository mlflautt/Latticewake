# Cycle 016 — expression input

Adds bounded glide/press/slide events to the fixed-capacity kernel ABI and
routes trackpad drags through it. This is a semantic input path, not callback
admission or MPE device support.
