# Cycle 015 — saved scene lifecycle

Loads or atomically creates a canonical Scene v0 file in Application Support,
shows a truncated SHA-256 receipt, and supplies those bytes to the kernel at
start. Per-note trackpad expression remains a later kernel-event extension.
